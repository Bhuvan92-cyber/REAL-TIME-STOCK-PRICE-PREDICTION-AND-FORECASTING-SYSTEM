version: "3.9"

services:
  stock-ml-app:
    build: .
    container_name: real_time_stock_prediction
    ports:
      - "8501:8501"
    volumes:
      - .:/app
    restart: unless-stopped
